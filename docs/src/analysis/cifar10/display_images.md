Module src.analysis.cifar10.display_images
==========================================

Functions
---------

`display_images(dataloader: torch.utils.data.dataloader.DataLoader, dim: tuple[int, int]) ‑> None`
:   Displays images in a grid.
    :param dataloader: Pytorch DataLoader object.
    :param dim: Dimensions of the grid, in the format: (rows, columns)
    :return: